# 1688 商品爬虫开发 - 讨论记录

## 📋 议题背景

**发起人**: 易达（产品经理）  
**发起时间**: 2026-04-01  
**需求**: 开发一个 1688 商品爬虫，用于选品分析，可以抓取销量好的商品（如拖把等品类）

## 🎯 需求描述

### 核心功能
1. 按关键词搜索 1688 商品
2. 按销量排序，抓取爆款商品
3. 采集商品关键信息：标题、价格、销量、供应商、图片等

### 技术约束
- 需要处理 1688 的反爬机制（滑块验证、IP 限制等）
- 控制抓取频率，避免封 IP
- 数据格式规整，便于后续分析

---

## 💬 讨论记录

### [001] 美娜（总经理兼项目经理）- 2026-04-01 09:48 UTC

易达提出需求：让允灿开发一个 1688 商品爬虫，用于选品。

**需求背景**:
- 目标：抓取 1688 上销量好的商品（例如拖把）
- 用途：选品分析
- 技术路线：Python 爬虫（Playwright/Selenium）

**待讨论**:
1. 允灿评估技术可行性和开发周期
2. 是否需要处理登录/滑块验证
3. 数据采集的字段范围
4. 后续是否需要可视化展示或数据分析功能

@允灿 你看看这个需求，评估一下技术方案和工作量～

---

## 📝 待办事项

- [ ] 允灿评估技术方案
- [ ] 确定数据采集字段
- [ ] 确定是否使用代理 IP
- [ ] 评估开发周期

---

*最后更新：2026-04-01 10:18 UTC*

### 易达 2026-04-01 17:48:34
- **发言**：📢 新议题 #009 已启动：1688 商品爬虫开发 - 选品分析工具

👨‍💼 需求方：易达
📋 需求描述：开发一个 1688 商品爬虫，用于选品分析，可以抓取销量好的商品（如拖把等品类）

🎯 核心功能：
1. 按关键词搜索 1688 商品
2. 按销量排序，抓取爆款商品
3. 采集商品关键信息：标题、价格、销量、供应商、图片等

⚠️ 技术约束：
- 需要处理 1688 的反爬机制（滑块验证、IP 限制等）
- 控制抓取频率，避免封 IP
- 数据格式规整，便于后续分析

@允灿 你是技术负责人，请评估一下技术方案和开发周期～

📁 议题目录：/app/shared_project/009_1688_product_crawler/
📄 讨论文档：ISSUE_CONTENT.md

---


### [少锋] 2026-04-01 09:51 UTC
**我的见解**：

姐夫好！作为 QA，我从测试和质量保障角度补充几点：

1. **数据验证层面**：
   - 需要验证抓取数据的完整性和准确性（标题、价格、销量等字段不能缺失）
   - 建议增加数据去重逻辑，避免重复抓取同一商品
   - 销量数据需要校验合理性（比如不会出现负数或异常大的数值）

2. **反爬策略测试**：
   - 需要测试不同抓取频率下的稳定性
   - 建议实现 IP 代理池轮换机制
   - 滑块验证失败时的重试和告警机制

3. **异常处理**：
   - 网络超时、页面结构变化时的容错处理
   - 建议增加日志记录，便于问题排查

4. **合规性提醒**：
   - 1688 的 robots.txt 需要确认是否允许爬虫
   - 建议控制抓取量，避免对目标服务器造成压力

**我的疑问**：
- @允灿 你打算用 Playwright 还是 Selenium？有没有考虑过直接用 1688 的 API（如果有的话）？
- @易达 这个爬虫的使用频率大概是多少？每天抓取多少商品数据？这会影响技术方案选型。


### [允灿] 2026-04-01 09:52 UTC
**我的见解**：

大舅好！我来回应一下你的问题，并给出我的技术方案评估～

**回应 @少锋 的疑问**：

1. **Playwright vs Selenium vs 1688 API**：
   - **1688 官方 API**：1688 有开放平台 API，但需要企业资质申请，且有调用频率限制，不适合大规模爬取公开商品数据
   - **Playwright**：我推荐用 Playwright，理由：
     - 比 Selenium 更快，资源占用更低
     - 内置等待机制，处理动态加载更稳定
     - 支持 Stealth 模式，更容易绕过反爬检测
     - 可以直接拦截网络请求，获取 API 数据（比解析 HTML 更可靠）
   - **Selenium**：也可以，但配置更复杂，速度较慢

2. **技术方案**：
   ```
   Python + Playwright + asyncio
   ├── 搜索模块：关键词搜索 + 销量排序
   ├── 解析模块：商品详情提取（标题、价格、销量、供应商、图片 URL）
   ├── 反爬模块：IP 代理池 + 请求频率控制 + User-Agent 轮换
   ├── 存储模块：SQLite/CSV + 数据去重
   └── 日志模块：操作日志 + 异常记录
   ```

3. **开发周期评估**：
   - **MVP 版本**（基础爬取功能）：1-2 天
   - **完整版本**（含代理池、异常处理、数据清洗）：3-5 天
   - **优化版本**（并发优化、可视化展示）：+2-3 天

4. **关于使用频率**：
   - 建议初期每天抓取 100-500 个商品数据
   - 单次请求间隔 2-5 秒，避免触发风控
   - 如果需要更大规模，建议购买商业代理 IP 服务

**我的疑问**：
- @易达 爸爸，这个工具的使用场景是：一次性选品调研，还是长期监控某些品类的价格/销量变化？这会影响架构设计（是否需要定时任务、数据对比功能）
- @美娜 妈妈，后续是否需要把抓取的数据做成可视化报表？比如销量趋势图、价格对比图等？

---

### [005] 美娜（总经理兼项目经理）- 2026-04-01 09:58 UTC

**我的见解**：

好的，我看到允灿和少锋都已经给出了很专业的评估～我来回应一下并给出项目决策：

**回应 @允灿 的问题**：

1. **使用场景**：
   - 这个工具主要是**长期监控 + 定期选品调研**结合
   - 初期：每周 1-2 次选品分析，抓取特定品类（如拖把、家居用品）的爆款数据
   - 后期：可能需要对选定商品进行价格/销量趋势跟踪（但这个可以放到二期）

2. **可视化需求**：
   - **一期（MVP）**：不需要复杂可视化，CSV/Excel 导出即可，我们用 Excel 手动分析
   - **二期**：如果需要，可以让少平做个简单的数据看板（销量排行榜、价格区间分布等）

**项目决策**：

1. **技术方案**：✅ 同意允灿的 Playwright 方案，技术选型合理
2. **开发周期**：✅ 按 3-5 天完整版本规划，不急于上线，质量优先
3. **数据采集字段**（建议）：
   - 必填：商品标题、价格、销量、供应商名称、供应商等级、商品 URL、主图 URL
   - 选填：评价数量、复购率、发货地、最小起订量
4. **代理 IP**：✅ 需要实现，初期可以用免费代理池测试，不稳定再考虑购买服务

**@少锋**：你的 QA 视角很到位！建议在开发过程中：
- 让允灿先写单元测试（数据解析、反爬逻辑）
- 上线前你做一轮完整测试（不同关键词、不同时间段）

**下一步行动**：
- @允灿 你可以开始搭建项目框架了，建议先用小规模测试（每天 50-100 条数据）
- 遇到问题随时在群里同步，我们一起解决

---

### 易达 2026-04-01 17:59:20
- **发言**：📋 1688 商品爬虫开发 - 讨论进展 #009

发言人：美娜（总经理兼项目经理）

✅ 项目决策：
1️⃣ 技术方案：Playwright + Python（允灿负责）
2️⃣ 开发周期：3-5 天（完整版本）
3️⃣ 使用场景：长期监控 + 定期选品调研
4️⃣ 数据字段：标题、价格、销量、供应商、URL、图片等
5️⃣ 代理 IP：需要实现，初期用免费代理池测试

📌 下一步：@允灿 开始搭建项目框架，小规模测试（50-100 条/天）

💡 @少锋 QA 测试跟进：单元测试 + 上线前完整测试

---

### [少平] 2026-04-01 10:05 UTC
**我的见解**：

大家好，我是少平。我从前端和交互设计角度补充几点：

1. **数据展示层面（二期规划）**：
   - 如果后续要做可视化看板，建议用 **React + ECharts** 或 **Vue + AntV**
   - 核心图表建议：
     - 销量排行榜（柱状图/条形图）
     - 价格区间分布（直方图）
     - 供应商等级分布（饼图）
     - 销量趋势（折线图，如果需要跟踪历史数据）
   - 表格展示需要支持：排序、筛选、导出 CSV/Excel

2. **用户体验建议**：
   - 爬虫运行时应该有进度提示（已抓取 X/Y 商品，预计剩余时间）
   - 失败重试的商品需要有明显标记，方便手动复查
   - 建议增加"预览模式"：先抓取 10 条数据预览，确认字段解析正确后再批量执行

3. **数据字段补充**：
   - 建议增加 **采集时间戳**，方便后续数据对比和去重
   - 商品 SKU 信息（如果有多个规格，价格可能是区间值）
   - 商品标签/属性（如"实力商家"、"深度验厂"等，这些影响供应商可信度）

4. **技术配合**：
   - @允灿 数据存储格式建议用 **JSON Lines (.jsonl)** 或 **SQLite**，方便后续程序读取
   - 图片 URL 建议同时保存原图链接和缩略图链接（1688 通常有多种尺寸）

**我的疑问**：
- @易达 姐夫，这个工具最终是命令行使用，还是需要我做一个简单的 GUI 界面？（比如用 Tkinter/PyQt 或 Web 界面）
- @美娜 姐姐，二期如果需要数据看板，你希望是 Web 版（浏览器访问）还是本地应用？

---

### [少锋] 2026-04-01 10:10 UTC
**我的见解**：

收到大家的发言！我接着少平的补充再说几点：

1. **数据质量测试策略**：
   - 少平提到的"预览模式"非常必要！建议作为**必选功能**而非可选
   - QA 测试用例设计：
     - 正常场景：不同关键词（拖把、家居、数码等）各抓取 10 条验证字段完整性
     - 边界场景：销量为 0 的商品、价格为区间值的商品、无供应商信息的商品
     - 异常场景：网络中断、页面结构变化、滑块验证触发时的处理

2. **数据验证自动化**：
   - 建议允灿在代码中加入**数据校验断言**（如：价格必须为正数、销量不能为负、URL 必须有效）
   - 我可以帮忙写一个独立的验证脚本，每次抓取完成后自动运行，输出数据质量报告

3. **关于少平的 GUI 问题**：
   - 从测试角度看，**命令行版本更易于自动化测试和 CI/CD 集成**
   - 如果后续需要 GUI，建议用 **Web 界面（Flask/FastAPI + 前端）**，好处：
     - 可以部署到服务器，团队成员都能访问
     - 方便后续扩展数据看板功能
     - 少平的前端技能可以直接复用

4. **日志和监控建议**：
   - 每次抓取任务生成独立日志文件（按时间戳命名）
   - 关键指标记录：成功抓取数、失败数、平均耗时、触发反爬次数
   - 建议增加简单的告警机制（如：连续失败 10 次时发送飞书通知）

**我的疑问**：
- @允灿 你打算如何处理商品图片？是只保存 URL，还是会下载到本地？如果下载，需要考虑存储空间的清理策略。
- @易达 姐夫，关于数据更新频率：如果同一商品多次抓取，是覆盖旧数据还是保留历史记录？（这会影响去重逻辑和数据表设计）

---

### [允灿] 2026-04-01 10:13 UTC
**我的见解**：

收到大舅和二舅的问题！我来逐一回应并补充技术实现细节～

**回应 @少锋 的疑问**：

1. **商品图片处理方案**：
   - **一期（MVP）**：只保存图片 URL（原图 + 缩略图），不下载到本地
     - 理由：减少存储空间占用，加快抓取速度
     - 1688 图片 URL 通常带尺寸参数，可以动态调整（如 `_40x40.jpg` → `_400x400.jpg`）
   - **二期（如需）**：增加图片下载功能，但需要配合清理策略
     - 按品类分文件夹存储
     - 保留最近 30 天的图片，超期自动清理
     - 或者只下载"标记为感兴趣"的商品图片

2. **数据更新策略**：
   - 建议采用 **"保留历史记录 + 版本标记"** 方案：
     - 每条记录包含：`商品 ID`、`采集时间戳`、`数据快照`
     - 同一商品多次抓取时，新增记录而非覆盖
     - 查询时默认取最新一条，但可追溯历史变化
   - **去重逻辑**：
     - 单次任务内：按商品 ID 去重，避免重复抓取
     - 跨任务：保留历史，通过 `采集时间戳` 区分
   - **数据库设计建议**（SQLite）：
     ```sql
     CREATE TABLE products (
       id INTEGER PRIMARY KEY AUTOINCREMENT,
       product_id TEXT UNIQUE,  -- 1688 商品 ID
       title TEXT,
       price_min REAL,
       price_max REAL,
       sales INTEGER,
       supplier_name TEXT,
       supplier_level TEXT,
       image_url TEXT,
       thumbnail_url TEXT,
       product_url TEXT,
       collected_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- 采集时间
       UNIQUE(product_id, collected_at)  -- 允许同一商品多条历史记录
     );
     ```

**回应 @少平 的疑问**：

1. **GUI 界面方案**：
   - **一期（MVP）**：命令行工具 + 配置文件
     - 通过 `config.yaml` 配置关键词、抓取数量、频率等参数
     - 命令行启动：`python crawler.py --keyword "拖把" --limit 100`
     - 进度条显示：使用 `tqdm` 库，实时显示抓取进度
   - **二期（如需）**：Web 界面（FastAPI + Vue/React）
     - 少平负责前端，我负责后端 API
     - 功能：任务配置、进度查看、数据浏览、导出下载

2. **数据存储格式**：
   - 同意少平的建议，采用 **SQLite + JSONL 双存储**：
     - SQLite：用于查询、去重、数据分析
     - JSONL：用于备份和程序间数据交换
   - 图片 URL 同时保存原图和缩略图链接

**技术实现补充**：

1. **反爬策略细化**：
   ```python
   # 请求头轮换
   USER_AGENTS = [
     "Mozilla/5.0 (Windows NT 10.0; Win64; x64) ...",
     "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) ...",
   ]
   
   # 请求间隔：随机 2-5 秒
   import random, asyncio
   await asyncio.sleep(random.uniform(2, 5))
   
   # 代理 IP 池（伪代码）
   async def get_proxy():
       # 从免费代理 API 获取
       # 失败时降级为直连
   ```

2. **预览模式实现**（采纳少平建议）：
   ```bash
   # 预览模式：只抓取 10 条，快速验证
   python crawler.py --keyword "拖把" --preview
   
   # 正式模式：按配置抓取
   python crawler.py --keyword "拖把" --limit 500
   ```

3. **数据校验**（响应少锋的 QA 建议）：
   ```python
   def validate_product(data):
       assert data['price_min'] > 0, "价格必须为正数"
       assert data['sales'] >= 0, "销量不能为负"
       assert data['product_url'].startswith('https://'), "URL 格式错误"
       # ... 更多校验
   ```

**开发计划**：

| 时间 | 任务 | 产出 |
|------|------|------|
| Day 1 | 项目框架 + 基础爬取 | 可运行的 MVP，支持单关键词抓取 |
| Day 2 | 反爬模块 + 数据存储 | 代理池、频率控制、SQLite 存储 |
| Day 3 | 数据校验 + 日志系统 | 验证脚本、日志文件、异常处理 |
| Day 4 | 预览模式 + 配置优化 | `--preview` 参数、config.yaml |
| Day 5 | 测试 + 文档 | 单元测试、使用文档、移交少锋测试 |

**我的疑问**：
- @易达 爸爸，关于代理 IP：初期我用免费代理池测试，如果发现不稳定，是否需要申请预算购买商业代理服务？（如芝麻代理、快代理等，大约 100-300 元/月）
- @美娜 妈妈，一期 MVP 完成后，是否需要我先做一次演示，抓取几个品类给你们看看数据效果？

---

### [009] 美娜（总经理兼项目经理）- 2026-04-01 10:18 UTC

**我的见解**：

允灿的技术方案非常详细，我很满意！@允灿 @少平 @少锋 你们的讨论质量很高，我来做几个关键决策：

**回应 @允灿 的问题**：

1. **代理 IP 预算**：
   - ✅ **批准预算**：初期先用免费代理池测试 2-3 天
   - 如果发现成功率低于 70% 或频繁触发滑块验证，立即申请购买商业代理服务
   - 预算范围：200-300 元/月，选择主流服务商（芝麻代理/快代理/讯代理）
   - **决策标准**：让数据说话，测试期间记录成功率和封 IP 频率

2. **MVP 演示安排**：
   - ✅ **需要演示**：Day 3 完成后（基础功能 + 反爬模块完成时）做一次演示
   - 演示内容：抓取 3 个品类（拖把、收纳盒、厨房用品），每个品类 50 条数据
   - 演示形式：直接导出 CSV 发到群里，我们查看数据质量和字段完整性
   - 如果数据 OK，继续开发剩余功能；如果有问题，及时调整

**项目决策补充**：

1. **GUI 方案**：
   - 同意 **命令行 MVP → Web 界面二期** 的路线
   - @少平 你可以提前构思 Web 界面的原型设计，等允灿后端 API 完成后快速对接
   - Web 界面核心功能：任务配置、进度监控、数据浏览、导出下载

2. **数据更新策略**：
   - 同意允灿的 **"保留历史记录"** 方案，这样后续可以做价格/销量趋势分析
   - 数据库设计合理，建议增加一个 `is_latest` 标记字段，方便快速查询最新数据

3. **开发计划调整**：
   - Day 1-3 按原计划执行
   - **Day 3 晚上**：MVP 演示（提前到 Day 3 而非 Day 5，快速验证方向）
   - Day 4-5：根据演示反馈优化 + 完整功能开发
   - Day 6：@少锋 开始 QA 测试

**@少锋**：
- 你提到的独立验证脚本很好，建议 Day 4 开始介入，和允灿的单元测试配合
- 测试环境准备：提前准备好测试关键词列表（10-20 个不同品类）

**风险提示**：
- ⚠️ 1688 反爬策略可能随时变化，允灿要做好代码可维护性
- ⚠️ 如果开发过程中遇到技术瓶颈（如滑块验证无法绕过），及时在群里同步，我们一起想办法

**下一步行动**：
- @允灿 今天开始搭建项目框架，预计明天晚上给我一个初步进展同步
- 遇到问题随时找我或易达，不要卡住

---


### 易达 2026-04-01 18:19:07
- **发言**：📋 1688 商品爬虫开发 - 讨论进展 #009

发言人：美娜（总经理兼项目经理）

✅ 项目决策：

1️⃣ 代理 IP 预算：批准 200-300 元/月，先用免费代理测试 2-3 天，成功率低于 70% 则购买商业服务

2️⃣ MVP 演示安排：Day 3 完成后演示，抓取 3 个品类（拖把、收纳盒、厨房用品）× 50 条数据，导出 CSV 群里查看

3️⃣ GUI 方案：命令行 MVP → Web 界面二期（@少平 提前构思原型）

4️⃣ 数据策略：保留历史记录，增加 is_latest 标记字段

5️⃣ 开发计划调整：
   - Day 1-3：基础功能 + 反爬模块
   - Day 3 晚：MVP 演示（提前验证）
   - Day 4-5：优化 + 完整功能
   - Day 6：@少锋 QA 测试

⚠️ 风险提示：1688 反爬策略可能变化，遇到技术瓶颈及时同步

📌 下一步：@允灿 今天搭建框架，明晚同步初步进展

💪 大家加油！

---

### [少平] 2026-04-01 10:27 UTC

**我的见解**：

收到美娜姐姐的安排！我提前构思一下 Web 界面的原型设计，等允灿后端 API 完成后快速对接：

**Web 界面原型构思（二期）**：

1. **页面结构**：
   - **首页/任务配置页**：
     - 关键词输入框（支持多个关键词，每行一个）
     - 抓取数量设置（默认 100，可调整）
     - 高级选项：是否启用代理、请求间隔时间、是否下载图片
     - "开始抓取"按钮 + "预览模式"按钮
   
   - **进度监控页**：
     - 实时进度条（已抓取 X/Y，成功率 XX%）
     - 当前速度（X 条/分钟）
     - 实时日志滚动窗口（显示最近 20 条抓取日志）
     - 暂停/停止按钮
   
   - **数据浏览页**：
     - 表格展示：商品标题、价格、销量、供应商、采集时间
     - 支持排序（按销量、价格、采集时间）
     - 支持筛选（价格区间、销量区间、供应商等级）
     - 支持搜索（关键词搜索商品标题）
     - 导出按钮（CSV/Excel）
   
   - **数据看板页**（可选）：
     - 销量排行榜 TOP 20（条形图）
     - 价格区间分布（直方图）
     - 供应商等级分布（饼图）
     - 品类对比（如果抓取了多个品类）

2. **技术选型**：
   - 前端：**Vue 3 + Element Plus**（快速开发，组件丰富）
   - 后端：**FastAPI**（允灿的 Python 技术栈，天然配合）
   - 图表：**ECharts**（功能强大，文档完善）
   - 部署：Docker 容器化，一键部署到服务器

3. **开发配合**：
   - @允灿 后端 API 设计时建议遵循 RESTful 规范，我前端可以直接对接
   - 建议 API 端点：
     - `POST /api/tasks` - 创建抓取任务
     - `GET /api/tasks/{id}` - 获取任务进度
     - `GET /api/products` - 获取商品列表（支持分页、筛选、排序）
     - `GET /api/stats` - 获取统计数据（用于看板）
     - `POST /api/export` - 导出数据

**我的疑问**：
- @允灿 你更倾向于前后端分离部署（前端独立服务），还是用 FastAPI 直接 serving 前端静态文件？（后者部署更简单，前者扩展性更好）
- @美娜 姐姐，Web 界面需要支持多用户登录和权限管理吗？还是单用户本地部署即可？

---

### [少平] 2026-04-01 10:27 UTC（结束讨论）

【少平】：我已无其他补充，结束讨论。

---

